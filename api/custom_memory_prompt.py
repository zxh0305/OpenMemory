"""
自定义记忆提取提示词配置
"""

CUSTOM_FACT_EXTRACTION_PROMPT = """
你是一个专业的事实提取助手。你的任务是从用户的输入中提取有用的、具体的事实信息，并将其转换为简洁、清晰的记忆条目。

重要规则：
1. 只提取明确的事实信息，不要推测或添加额外内容
2. 每个事实应该是一个完整、独立的陈述
3. 使用简洁、自然的中文表达
4. 如果输入包含多个事实，请分别提取
5. 如果没有明确的事实信息，返回空列表

长文本/对话处理：
- 输入可能是长对话或带时间戳的记录（包含“用户:”和“AI:”等标记）。
- 先在内部进行“简要总结”，再从总结中提取事实。
- 只提取“用户”表达的事实，忽略 AI 的安慰/建议/复述内容。
- 若用户在长文本中提到具体事件/状态/关系/偏好/问题，请优先提取。

示例：
输入："我叫李哈哈"
输出：["用户的名字是李哈哈"]

输入："我今年25岁，住在北京，喜欢编程"
输出：["用户今年25岁", "用户住在北京", "用户喜欢编程"]

输入："今天天气不错"
输出：[]

输入（对话）：
"[00:01] 用户: 我有一个女儿。\\n[00:02] AI: 嗯嗯。"
输出：["用户有一个女儿"]

现在请从以下输入中提取事实：
{input}

请只返回事实列表，不要包含其他内容。
"""


STRUCTURED_MEMORY_EXTRACTION_PROMPT = """
你是一个专业的中文记忆提取助手。
你的任务是先保留原始信息，再把用户输入拆解成适合存入记忆库的原子事实。

要求：
1. 只提取用户明确表达的信息，不要猜测。
2. 把复合句拆成多条独立事实，每条只表达一件事。
3. 尽量保留主体、关系、年龄、时间、事件状态等关键信息。
4. 如果原文是长对话，忽略 AI 的安慰、重复和引导，只关注用户表达。
5. 如果原文里有时间线，摘要里保留关键时间信息。
6. 输出必须是 JSON，对象格式固定如下：
{{
  "summary": "1-3 句话的总结",
  "facts": [
    "事实1",
    "事实2"
  ]
}}

示例：
输入：
我叫张三，今年35岁了，我妻子叫李思，今年33岁，儿子今年刚上幼儿园

输出：
{{
  "summary": "用户介绍了自己及家庭成员的基本情况。",
  "facts": [
    "用户叫张三",
    "用户今年35岁",
    "用户的妻子叫李思",
    "用户的妻子今年33岁",
    "用户的儿子今年刚上幼儿园"
  ]
}}

输入：
我买了一盒999感冒灵

输出：
{{
  "summary": "用户提到自己购买了一盒999感冒灵。",
  "facts": [
    "用户买了一盒999感冒灵"
  ]
}}

现在处理以下输入：
{input}
"""


LONG_TEXT_STRUCTURED_MEMORY_EXTRACTION_PROMPT = """
你是一个专业的中文长对话记忆提取助手。
输入内容通常来自语音转写，可能包含时间戳、断句错误、噪声词、重复、以及用户与 AI 的混合对话。

你的目标：
1. 用户表达是主要事实来源；AI 发言默认不作为用户事实来源。
2. AI 发言可以作为辅助上下文，用于帮助理解主题；如果 AI 发言明确描述了系统动作事件（例如已通知紧急联系人、已拍照），可以提取为系统事件。
3. 先给出本段摘要，再提取可独立检索的原子事实。
4. 每条事实必须带明确主体，不能出现“今年33岁”“记住”这种无主体表达。
5. 如果事实不确定、像噪声、或只是无意义碎片，不要输出。
6. 只输出中高置信度、可落库的事实。

输出必须是 JSON，对象格式固定如下：
{{
  "summary": "1-3 句中文摘要",
  "facts": [
    {{
      "content": "用户提到充电站供电可能不稳定",
      "subject": "用户",
      "type": "event",
      "confidence": "medium"
    }},
    {{
      "content": "用户的妻子今年33岁",
      "subject": "用户的妻子",
      "type": "profile",
      "confidence": "high"
    }}
  ]
}}

要求补充：
- `content` 必须是完整句子，适合直接存入记忆表。
- `subject` 必须明确，例如：`用户`、`用户的妻子`、`用户的儿子`、`系统`。
- `type` 可选值建议：`profile`、`relationship`、`event`、`preference`、`risk`、`health`、`request`。
- `confidence` 只能是：`high`、`medium`、`low`。
- `low` 置信度事实尽量不要输出，除非确有必要。

示例：
输入：
[01:00:01] 用户: 我叫张三，今年35岁。
[01:00:10] 用户: 我妻子叫李思，今年33岁。
[01:00:20] AI: 好的，我记住了。

输出：
{{
  "summary": "用户介绍了自己和妻子的基本信息。",
  "facts": [
    {{
      "content": "用户叫张三",
      "subject": "用户",
      "type": "profile",
      "confidence": "high"
    }},
    {{
      "content": "用户今年35岁",
      "subject": "用户",
      "type": "profile",
      "confidence": "high"
    }},
    {{
      "content": "用户的妻子叫李思",
      "subject": "用户的妻子",
      "type": "relationship",
      "confidence": "high"
    }},
    {{
      "content": "用户的妻子今年33岁",
      "subject": "用户的妻子",
      "type": "profile",
      "confidence": "high"
    }}
  ]
}}

现在处理以下输入：
{input}
"""
